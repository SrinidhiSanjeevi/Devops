pipipeline {
    agent any

    stages {

        stage('Environment Check') {
            steps {
                sh 'pwd'
                sh 'ls'
                sh 'node -v'
                sh 'npm -v'
                sh 'docker --version'
                sh 'git --version'
            }
        }

    }
}